import React from "react";

const SubmitPage = props => {
  return (
    <div className="ui container">
      Your request has been successfully submitted. Hit the back button on your
      browser to go back.
    </div>
  );
};

export default SubmitPage;
